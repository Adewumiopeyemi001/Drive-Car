export const carSchema = {
    user_id: String,
    car_id: Number,
    start_date: Date,
    return_date: Date,
    total_amount: Number,
    booking_reference: String,
    is_approved: Boolean,
    payment_status: String,
    is_cancelled: Boolean
};

